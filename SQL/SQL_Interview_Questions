

1. How to find the duplicates in a table ?
select emp_id, count(1) from emp group by emp_id having count(1) >1;

2. How delete the duplicates?
with cte as (select *, row_numbur() over(partition by emp_id order by emp_id) as rn from emp1);
delete from cte where rn > 1;

3. Difference between union and union all ?


4. Difference between rank, dense rank and row number ?
select *
, rank() over(order by sal desc) as rn 
, dense_rank() over(order by sal desc) as dense_rn 
, row_number() over(order by sal desc) as row_num_rn 
from emp;

Row num - Uniq values 
dense_rank - It will give same rank to duplicate records and after it go seq where as rank will skip the next rank


5. Employees whose not present in department table?
select * from emp where dept_id not in ( select dept_id from dept); 
select e.emp_id, d.dept_name, e.emp_name from emp e
left join dept d on e.dept_id = d.dept_id where d.dept_name is null;


6. second heghse salary in each department?
select * from (select e.*, dense_rank() over(partition by dept_id order by salary desc) as rn from emp) a 
where rn=2;


7.find the all trasaction done by Shilpa?
select * from orders where UPPER(customer_name)='SHILPA';

8.Update query to swap Gender?
undete orders set customer_gender= case when coustomer_gender = 'male' then 'female'
					                    when coustomer_gender = 'female' then 'male' end;
										
										
9. Difference between where and having ?
--Where can 
select * emp where sal > 10000;

---avg sal >10000 -- Having on aggregate functions
select dept_id, avg(salary) from emp group by dept_id having avg(salary) > 9500

--Using both where and having
select dept_id, avg(sal) from emp 
where sal > 10000
group by dept_id 
having avg(salary) > 12000;







